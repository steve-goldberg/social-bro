# Database (PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/social_bro"

# Auth (NextAuth v5) - generate with: openssl rand -base64 32
AUTH_SECRET=""

# Encryption for stored API keys - generate with: openssl rand -base64 32
ENCRYPTION_SECRET=""

# Admin invite system
ADMIN_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Optional: Fallback API keys (users can also add their own in Settings)
# YOUTUBE_API_KEY=""
# RAPIDAPI_KEY=""
# OPENROUTER_API_KEY=""
